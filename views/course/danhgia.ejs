<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/danhgia.css">
    <title>Document</title>
    <script src="//code.jquery.com/jquery-1.11.1.min.js"></script>
</head>

<body>
    <% data.forEach(function(data) { %>
    
        <form action="/course/dodanhgiabaibao<%= data._id %>" method="POST">

            <p>bạn có thể dowload file tại đây<a href="/download/<%= data._id %>"><%= data.nameFile %></a></p>
            <iframe src="/<%= data.filePath %>" class="viewfiles"></iframe>

            <%if (data.filePathAnh) { %>
                <img width="40px" height="40px" src="/<%= data.filePathAnh %>"/>
            <% }  %>
            
            <div class="swap">
                <p>Status: 
            <select name="status" required>
                <option value="<%= data.status %>"><%= data.status %></option>
                <option value="Fail">Fail</option>
                <option value="Pass">Pass</option>
            </select>
            </p>
            <p>Comment: </p><textarea name="comment" id="" cols="56" rows="20" value="<%= data.comment %>" required><%= data.comment %></textarea>
            <br>

            <button type="submit" class="button">Submit</button>
            </div>

    </form>
    <% }); %>

</body>

</html>