var fs = require('fs');

/**
 * Create a file stream.
 *
 * @param {String} filename
 * @param {Object} options (optional)
 * @return {ReadableStream} stream
 * @api public
 */

module.exports = function (filename, options) {
    return fs.createReadStream(fs, arguments);
};

